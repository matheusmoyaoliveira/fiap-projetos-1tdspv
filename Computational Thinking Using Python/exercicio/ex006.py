# Escreva um algoritmo que calcula a área e o perímetro do círculo, use 3.141592 como valor de π

pi = 3.14

raio = float(input('Digite o valor do raio à ser calculado: '))

area = pi * raio ** 2

print('A área do circulo é {:.2f}'.format(area))
